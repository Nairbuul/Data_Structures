#include <cmath>
#include <iostream>
#include <iomanip>
#include <fstream>
#include <set>
#include <vector>
#include <cstring>
#include <string.h>

#include "includes/tokenizer/constants.h"
#include "includes/tokenizer/stokenize.h"

using namespace std;

int main(int argv, char** argc) {
    cout << "\n\n"
         << endl;

    // STokenizer tkn();

    // char a[MAX_BUFFER]; 
    // ifstream s;
    // s.open("solitude_mini.txt");
    
    // if(s.is_open()){
        
        // s.read(a, 100);
        // cout << a << endl;
        // while(s.peek() != EOF){
        // // for(int i = 0; i < 2; i++){
        //     s.read(a, MAX_BUFFER-1);
        //     a[s.gcount()] = '\0';
        //     cout << a ;//<< endl;
            
        // }
        // s.read(a, MAX_BUFFER);
        // cout <<  s.gcount() <<setw(5) << endl << endl; //reads the amount of bits there are in teh new array.
        // a[s.gcount()] = '\0';
        // cout << strlen(a) << endl << endl;;
        // cout << a << endl;
    // }

    cout << "\n\n\n=====================" << endl;
    return 0;
}
