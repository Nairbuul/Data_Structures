#include <iostream>
#include <iomanip>
// #include <algorithm>
// #include <cctype>
// #include <string>
// #include "includes/stl_utils/string_utilities.h"

using namespace std;

int main(int argv, char** argc) {
    cout << "\n\n"
         << endl;

    // string s = "LOWER CASE ME: ";
    // transform(s.begin(), s.end(), s.begin(), [](unsigned char c){return std::tolower(c);});
    // cout << s << endl;

    

    // std::string data = "LOWER";
    // std::transform(data.begin(), data.end(), data.begin(),
    // [](unsigned char c){ return std::tolower(c); });
    // cout << data << endl;
    // // cout << s << '\t' << to_lower(s) << endl;

    string a = "AAAAAAAAA";
    // string b = to_lower(a);
    // cout << b << endl;

    // string s = "FUNC: CALL";
    // cout <<"" << endl;

    cout << "\n\n\n=====================" << endl;
    return 0;
}
